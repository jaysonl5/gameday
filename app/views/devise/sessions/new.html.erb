<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Sign In - GameDay Men's Health</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
        min-height: 100vh;
        background: linear-gradient(135deg, #fff 0%, #333 100%);
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 20px;
      }

      .container {
        width: 100%;
        max-width: 420px;
      }

      .logo-container {
        text-align: center;
        margin-bottom: 2rem;
      }

      .logo-container img {
        width: 200px;
        max-width: 100%;
      }

      .card {
        background: white;
        border-radius: 8px;
        padding: 2rem;
        box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
      }

      .title {
        font-size: 1.5rem;
        font-weight: 700;
        text-align: center;
        margin-bottom: 0.5rem;
        color: #1a1a1a;
      }

      .subtitle {
        text-align: center;
        color: #666;
        font-size: 0.875rem;
        margin-bottom: 1.5rem;
      }

      .alert {
        background-color: #fee;
        border: 1px solid #fcc;
        border-radius: 4px;
        padding: 0.75rem;
        margin-bottom: 1rem;
        color: #c00;
        font-size: 0.875rem;
      }

      .alert-icon {
        display: inline-block;
        margin-right: 0.5rem;
      }

      .google-btn {
        width: 100%;
        padding: 0.75rem;
        border: 1px solid #ddd;
        border-radius: 4px;
        background: white;
        color: #333;
        font-size: 1rem;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        text-decoration: none;
        margin-bottom: 1rem;
        transition: background-color 0.2s;
      }

      .google-btn:hover {
        background: #f8f9fa;
      }

      .google-icon {
        width: 20px;
        height: 20px;
        margin-right: 0.5rem;
      }

      .divider {
        display: flex;
        align-items: center;
        text-align: center;
        margin: 1.5rem 0;
        color: #666;
        font-size: 0.875rem;
      }

      .divider::before,
      .divider::after {
        content: '';
        flex: 1;
        border-bottom: 1px solid #ddd;
      }

      .divider span {
        padding: 0 1rem;
      }

      .form-group {
        margin-bottom: 1rem;
      }

      .form-label {
        display: block;
        margin-bottom: 0.5rem;
        color: #333;
        font-size: 0.875rem;
        font-weight: 500;
      }

      .form-input {
        width: 100%;
        padding: 0.75rem;
        border: 1px solid #ddd;
        border-radius: 4px;
        font-size: 1rem;
        transition: border-color 0.2s;
      }

      .form-input:focus {
        outline: none;
        border-color: #667eea;
      }

      .submit-btn {
        width: 100%;
        padding: 0.75rem;
        background: #667eea;
        color: white;
        border: none;
        border-radius: 4px;
        font-size: 1rem;
        font-weight: 600;
        cursor: pointer;
        transition: background-color 0.2s;
        margin-top: 0.5rem;
      }

      .submit-btn:hover {
        background: #5568d3;
      }

      .footer-links {
        text-align: center;
        margin-top: 1.5rem;
        font-size: 0.875rem;
      }

      .footer-links a {
        color: #667eea;
        text-decoration: none;
      }

      .footer-links a:hover {
        text-decoration: underline;
      }

      .footer-text {
        text-align: center;
        margin-top: 2rem;
        color: white;
        font-size: 0.75rem;
      }

      .text-muted {
        color: #666;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="logo-container">
        <img src="/assets/images/logo.svg" alt="GameDay Logo" />
      </div>

      <div class="card">
        <h2 class="title">Welcome Back</h2>
        <p class="subtitle">Sign in to access your dashboard</p>

        <% if flash[:alert].present? %>
          <div class="alert">
            <span class="alert-icon">⚠️</span>
            <%= flash[:alert] %>
          </div>
        <% end %>

        <% if devise_mapping.omniauthable? && resource_class.omniauth_providers.include?(:google_oauth2) %>
          <%= button_to "Sign in with Google", 
    user_google_oauth2_omniauth_authorize_path,  method: :post %>

          <div class="divider">
            <span>or continue with email</span>
          </div>
        <% end %>

        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="form-group">
            <%= f.label :email, class: "form-label" %>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-input", placeholder: "your@email.com" %>
          </div>

          <div class="form-group">
            <%= f.label :password, class: "form-label" %>
            <%= f.password_field :password, autocomplete: "current-password", class: "form-input", placeholder: "Your password" %>
          </div>

          <% if devise_mapping.rememberable? %>
            <div class="form-group" style="display: flex; align-items: center;">
              <%= f.check_box :remember_me, style: "margin-right: 0.5rem;" %>
              <%= f.label :remember_me, style: "margin: 0; font-weight: normal;" %>
            </div>
          <% end %>

          <%= f.submit "Sign In", class: "submit-btn" %>
        <% end %>

        <div class="footer-links">
          <p class="text-muted" style="margin-bottom: 0.5rem;">
            Don't have an account? <%= link_to "Sign up", new_registration_path(resource_name) %>
          </p>
          <p style="font-size: 0.75rem;">
            <%= link_to "Forgot password?", new_password_path(resource_name) %>
          </p>
        </div>
      </div>

      <p class="footer-text">
        © 2025 GameDay Men's Health. All rights reserved.
      </p>
    </div>
  </body>
</html>
